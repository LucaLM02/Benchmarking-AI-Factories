# Requisiti per l'ambiente locale (automazione e visualizzazione)

# Include i requisiti di base necessari anche per l'esecuzione remota
-r requirements-core.txt

# --- API / Orchestration (per il server locale) ---
fastapi>=0.110.0
uvicorn>=0.29.0

# --- Report generation (per la visualizzazione locale) ---
matplotlib>=3.8.0
reportlab>=4.0.9