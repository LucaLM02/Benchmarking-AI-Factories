# MeluXina cluster connection settings.
# Customize these values before running scripts/meluxina_benchmark.sh.

PROJECT_ID="p200981"

# Slurm allocation defaults
SLURM_PARTITION="gpu"
SLURM_TIME_LIMIT="01:00:00"

# Remote workspace/project configuration
REMOTE_WORKSPACE="/project/scratch/${PROJECT_ID}/${USER}/benchmarks"
REMOTE_PROJECT_DIR="${REMOTE_WORKSPACE}/Benchmarking-AI-Factories"
RECIPE_PATH="Recipes/Meluxina_DataIngestionRecipe.yaml"
REPO_URL="git@github.com:LucaLM02/Benchmarking-AI-Factories.git"

# Local path to store collected artifacts
LOCAL_RESULTS_DIR="${HOME}/meluxina-results"

# Grafana port on localhost when visualizing results
GRAFANA_PORT=3010
